#include "monty.h"
/**
 * f_stack - prints the top element of the stack
 * @head: ptr to the head stack
 * @counter: line_number to be processed
 * Return: no return
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
